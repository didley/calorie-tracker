{
  "name": "calorie-tracker",
  "author": "DoneDidley",
  "license": "MIT",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all client:prestart -p client api",
    "test-env": "npm-run-all client:prestart -p client api:test",
    "client": "npm-run-all client:prestart client:start",
    "client:prestart": "npm run prestart --prefix client_web/",
    "client:start": "npm start -prefix client_web/",
    "api": "npm start --prefix api/",
    "api:test": "npm run test --prefix api/"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
