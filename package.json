{
  "name": "calorie-tracker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "client_web": "file:client_web"
  },
  "scripts": {
    "start": "npm-run-all prestart -p start:*",
    "prestart": "npm run prestart --prefix client_web/",
    "start:client": "npm start --prefix client_web/",
    "start:server": "npm start --prefix server/",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "proxy": "http://localhost:5000"
}
